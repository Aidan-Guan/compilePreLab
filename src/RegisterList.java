import java.util.HashMap;

public class RegisterList {
    // 存储一个函数下的寄存器最大值
    static HashMap<String, Integer> curRegIndex = new HashMap<>();
    // 存储一个函数下的变量和寄存器的对照表
    static HashMap<String, HashMap<String, Integer>> varRegMap = new HashMap<>();


}

